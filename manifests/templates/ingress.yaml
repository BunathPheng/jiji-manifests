---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: jiji                  # Ingress name
  labels:
    app: jiji
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "jiji.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: jiji               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
